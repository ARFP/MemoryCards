<UserControl x:Class="MemoryCards.ButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemoryCards"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BitmapImage x:Key="woodBlack" UriSource="/Images/woodBlack.png"/>
    </UserControl.Resources>
    <Grid x:Name="UserControlCard">
        <Button x:Name="ButtonCard" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}" Command="{Binding Path=commandDisplay}" MinWidth="160" MinHeight="160">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                                    <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="20" BorderThickness="2">
                                        <Border.BorderBrush>
                                            <ImageBrush ImageSource="{StaticResource woodBlack}"/>
                                        </Border.BorderBrush>
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding Path=VMCard.CardImage, UpdateSourceTrigger=PropertyChanged}" Stretch="Uniform"/>
                                        </Border.Background>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
